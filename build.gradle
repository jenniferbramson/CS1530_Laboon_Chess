apply plugin: 'java'
apply plugin: 'application'
mainClassName = "chess.HelloWorld"
mainClassName = "chess.LaboonChess"

repositories {
  mavenCentral()
}

dependencies {
  testCompile 'junit:junit:4.12'
}

test {
	testLogging{
		events 'passed'
	}
}

sourceSets {
  main {
     output.resourcesDir = "build/classes/main"
   }
}

task plumbus << {
  println "A plumbus task!"
}
